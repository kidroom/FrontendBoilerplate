---
alwaysApply: true
---

# 元件(component)實作
- 整合需求寫出高維護性且具拓展性的程式 所有基礎元件的樣式風格都要一致
  
## 1. 表單查詢-查詢條件區塊
1. 可以放入不同的元件 textbox、label、radio button群組、下拉選單、checkbox群組等
2. 可以帶入參數調整排版。例如:查詢條件會有一個label搭配textbox，label與textbox並排或是換行顯示。如有其他的建議可以用其他方式展現，例如把label與textbox弄成一個component。但排版還是需要提供參數調整，一行幾個元件及元件排列方式並排、換行等。

## 2. 分頁
1. 實作分頁邏
2. 分頁元件需要顯示共幾筆目前是幾筆~幾筆
    1. 帶入參數選擇顯示/不顯示 預設不顯示
3. 分頁功能元件組成:
   1. 完整實作功能需要第一頁、上一頁、顯示當下頁數前後共10頁的數字供點選、下一頁、最後一頁
   2. 第一頁、最後一頁 為一組可以帶入參數選擇顯示/不顯示 預設不顯示
   3. 顯示當下頁數前後共10頁的數字供點選如果大於十頁 假設頁數是1~5頁就顯示1~10到 到第6頁是2~11 如果總頁數是30 25~30都是顯示21~30 在24頁顯示的是20~29 這功能也帶入參數選擇顯示/不顯示 預設不顯示
   4. 第一頁、上一頁、下一頁、最後一頁需要統一樣式與風格 且有多個不同樣式的按鈕可以切換圖示顯示、文字顯示兩種 預設圖示按鈕
   5. 如果資料只有一頁則不顯示 第一頁、上一頁、顯示當下頁數前後共10頁的數字供點選、下一頁、最後一頁 這個區塊
4. 跳選至某指定分頁功能: 
   1. 幫我實作多個樣式供切換 帶入參數選擇顯示/不顯示 預設不顯示 多個樣式供切換 預設樣式一
   2. 樣式一: 下拉選單選擇頁數、選單內容是總頁數的列表1~最後一頁 當選擇頁數後就直接觸發換頁
   3. 樣式二: 一個textbox跟button為一個組合 如果有好的推薦設計可以以改動 手動填入頁數 填選完後按下enter或按下按鈕觸發換頁
5. 可選擇每頁顯示資料量 及時調整顯示資料
6. 每頁筆數、跳往指定分頁、分頁顯示的樣式皆可帶入參數調整

## 3. 表單查詢-查詢結果區塊
1. 需要有查詢結果區塊 顯示回傳資料
2. 需要包含分頁component並且實作分頁
3. 可以傳入table header 並依照傳入資料自動mapping到各自欄位
4. 欄位可以調整顯示內容 文字、按鈕、checkbox、圖片、圖案等 
5. table header 一律垂直置中 但每個欄位的table header可以帶入參數選擇靠左、置中、靠右 預設靠左
6. 每一列的資料欄位一律垂直置中 但可以各自選擇靠左、置中、靠右 預設靠左

## 4. radio button群組
1. 帶入陣列資料顯示多個radio選項
   
## 5. 下拉選單
1. 帶入陣列資料顯示下拉選單
   
## 6. checkbox群組 
1. 帶入陣列資料顯示多個checkbox選項

## 7. 檔案上傳
1. label + input + image + btn 為最基礎的設定 如果有什麼其他配置可以提供配置 
2. image提供上傳功能不顯示 
3. input顯示上傳檔案名稱 
4. label顯示標題 可帶入參數調整顯示會不顯示
5. btn為上傳按鈕 選擇上傳檔案 呼叫image
