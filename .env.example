# ---------------------------------------------------------------------------
# 重要：Vite 不會載入本檔（.env.example 僅供說明與複製範本）。
# 開發請依賴專案內已提供的 .env.development；
# 建置請依賴 .env.production / .env.staging，或由 CI 注入變數。
# 本機覆寫（含密鑰）請用 .env.local 或 .env.[mode].local（已列入 .gitignore）。
# ---------------------------------------------------------------------------

# 後端 API 基底 URL（Axios baseURL）
VITE_API_BASE_URL=http://localhost:3000/api
